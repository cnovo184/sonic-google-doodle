gsap.to(".googleLetter",{
    duration: 1,     // Duration of each animation
   y: 50,           // Move each letter 50 pixels on the y-axis
   stagger: 0.2,    // Stagger start time by 0.2 seconds for each letter
   repeat: -1,
   ease: "power1.inOut"
 });